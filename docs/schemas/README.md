# JSON schemas (documentation)

These files describe the on-wire shape of NATS envelopes. **Go source of truth** is [pkg/harvest](../../pkg/harvest/) and [pkg/commit](../../pkg/commit/). When you change those packages, update the matching schema here in the same PR.

**Canonical copy (cxado meta-repo):** [shared/contracts/](https://github.com/butbeautifulv/cxado/tree/main/shared/contracts) — update engage wire schemas there first, then mirror here for standalone veil clones.
